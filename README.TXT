CQ3 Analysis

Mini-Max Sum

Most of this function is fairly simple. Java's "sort" function takes up most of the program's time as it's best and worst case is O(n*log(n)), making this the function's time complexity.

HackerRank in a String!

The time complexity for this function is O(n) as the program will continue to run depending on the length of the input string 's'. The function does constant work depending on each character of s.